# Mid-project break tests

This folder contains exactly six tests:

- Three tests for task comments
- Three tests for search and combined filters

Run only these tests from the project root:

```cmd
pytest -q Mid_project_test.py
```

The tests use a temporary JSON file and do not modify `data/tasks.json`.
